# 🎉 Добро пожаловать в RC2App!

## 👋 С чего начать?

Вы находитесь в полностью настроенном React Native проекте без Expo.
Всё готово к разработке!

## 🚀 Быстрый старт (3 минуты)

### 1. Установите зависимости
```bash
npm install
```

### 2. Для iOS (только macOS)
```bash
cd ios && pod install && cd ..
```

### 3. Запустите приложение

**Android:**
```bash
npm start          # Терминал 1
npm run android    # Терминал 2
```

**iOS:**
```bash
npm start      # Терминал 1
npm run ios    # Терминал 2
```

## 📚 Полезные документы

Выберите документ в зависимости от ваших целей:

### Для быстрого старта:
📖 **[QUICKSTART.md](./QUICKSTART.md)** - Подробная инструкция по запуску

### Для понимания проекта:
📁 **[PROJECT_STRUCTURE.md](./PROJECT_STRUCTURE.md)** - Структура и организация кода
✨ **[FEATURES.md](./FEATURES.md)** - Полный список возможностей

### Для разработки:
📘 **[README.md](./README.md)** - Основная документация
🤝 **[CONTRIBUTING.md](./CONTRIBUTING.md)** - Правила разработки
📦 **[VERSION_INFO.md](./VERSION_INFO.md)** - Информация о версиях

## 🎯 Что уже есть в проекте?

✅ **React Native 0.82.1** без Expo
✅ **TypeScript** с строгой типизацией
✅ **ESLint + Prettier** для качества кода
✅ **Готовые UI компоненты** (Button, Card, TextInput)
✅ **API сервис** для HTTP запросов
✅ **Утилиты и константы**
✅ **Организованная структура папок**
✅ **Алиасы путей** (@components, @screens, etc.)
✅ **Полная документация**

## 💡 Первые шаги

### 1. Изучите HomeScreen
Откройте `src/screens/HomeScreen.tsx` - это ваш стартовый экран.

### 2. Посмотрите компоненты
Проверьте `src/components/` - там готовые UI компоненты.

### 3. Используйте алиасы
Вместо:
```typescript
import {Button} from '../../../components/Button';
```

Пишите:
```typescript
import {Button} from '@components';
```

### 4. Настройте API
Откройте `src/services/api.ts` и измените `API_BASE_URL`.

### 5. Добавьте свой экран
Создайте файл в `src/screens/` и начните разработку!

## 🛠 Полезные команды

```bash
npm start              # Запуск Metro bundler
npm run android        # Запуск на Android
npm run ios            # Запуск на iOS
npm run lint           # Проверка кода
npm run lint:fix       # Исправление ошибок
npm run format         # Форматирование кода
npm run type-check     # Проверка типов TypeScript
npm test               # Запуск тестов
```

## 📱 Структура папок src/

```
src/
├── assets/       # Изображения, шрифты
├── components/   # UI компоненты
├── screens/      # Экраны приложения
├── navigation/   # Навигация (для React Navigation)
├── services/     # API и сервисы
├── utils/        # Утилиты, константы
└── types/        # TypeScript типы
```

## 🎨 Примеры кода

### Создание компонента
```typescript
import React from 'react';
import {Button} from '@components';
import {COLORS} from '@utils';

export const MyScreen = () => {
  const handlePress = () => {
    console.log('Pressed!');
  };

  return <Button title="Нажми меня" onPress={handlePress} />;
};
```

### API запрос
```typescript
import {api} from '@services';

const fetchData = async () => {
  try {
    const data = await api.get('/endpoint');
    console.log(data);
  } catch (error) {
    console.error(error);
  }
};
```

## 🐛 Проблемы?

Если что-то не работает:

1. **Очистите кеш:**
   ```bash
   npm run reset-cache
   ```

2. **Переустановите зависимости:**
   ```bash
   npm run clean
   ```

3. **Проверьте документацию:**
   - [README.md](./README.md) - раздел "Решение проблем"

## 🎓 Следующие шаги

### Рекомендуем добавить:

1. **Навигацию** (React Navigation)
   ```bash
   npm install @react-navigation/native @react-navigation/native-stack
   ```

2. **State Management** (Zustand/Redux/MobX)
3. **Async Storage** для локального хранения
4. **React Query** для работы с API
5. **i18n** для интернационализации

## 💬 Нужна помощь?

- 📖 Читайте полную документацию в [README.md](./README.md)
- 📁 Изучите структуру в [PROJECT_STRUCTURE.md](./PROJECT_STRUCTURE.md)
- ✨ Смотрите возможности в [FEATURES.md](./FEATURES.md)

## 🎉 Готово!

Проект полностью настроен и готов к разработке.
**Удачи в создании вашего приложения!**

---

**Совет:** Сохраните этот файл в закладки - здесь собраны все важные ссылки.

**Happy Coding! 🚀**

