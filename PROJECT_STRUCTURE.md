# Структура проекта RC2App

Этот документ описывает организацию файлов и папок в проекте.

## 📁 Корневая директория

```
RC2App/
├── android/                    # Android нативный код
├── ios/                        # iOS нативный код
├── src/                        # Исходный код приложения
├── __tests__/                  # Тесты
├── node_modules/               # Зависимости (git ignored)
├── .editorconfig              # Конфигурация редактора
├── .eslintrc.js               # ESLint конфигурация
├── .gitignore                 # Git ignore правила
├── .nvmrc                     # Node version manager
├── .prettierrc.js             # Prettier конфигурация
├── App.tsx                    # Корневой компонент приложения
├── app.json                   # React Native конфигурация
├── babel.config.js            # Babel конфигурация с алиасами
├── CONTRIBUTING.md            # Правила контрибуции
├── Gemfile                    # Ruby зависимости (для iOS)
├── index.js                   # Точка входа
├── jest.config.js             # Jest конфигурация
├── metro.config.js            # Metro bundler конфигурация
├── package.json               # NPM зависимости и скрипты
├── PROJECT_STRUCTURE.md       # Этот файл
├── react-native.config.js     # React Native CLI конфигурация
├── README.md                  # Основная документация
└── tsconfig.json              # TypeScript конфигурация
```

## 📂 Директория src/

Вся бизнес-логика и UI компоненты находятся здесь.

### src/components/

Переиспользуемые UI компоненты.

```
components/
├── Button.tsx                 # Кастомная кнопка
├── Card.tsx                   # Компонент карточки
├── TextInput.tsx              # Кастомный текстовый инпут
└── index.ts                   # Экспорты компонентов
```

**Правила:**
- Один компонент = один файл
- Используйте PascalCase для имен
- Экспортируйте через index.ts
- Определяйте Props интерфейсы

**Пример:**
```typescript
interface ButtonProps {
  title: string;
  onPress: () => void;
}

export const Button: React.FC<ButtonProps> = ({title, onPress}) => {
  // ...
};
```

### src/screens/

Экраны приложения (страницы).

```
screens/
├── HomeScreen.tsx             # Домашний экран
└── index.ts                   # Экспорты экранов
```

**Правила:**
- Один экран = один файл
- Суффикс "Screen" в имени
- Экспортируйте через index.ts

### src/navigation/

Навигация приложения (React Navigation).

```
navigation/
├── RootNavigator.tsx          # Корневой навигатор
├── types.ts                   # Типы навигации
└── README.md                  # Инструкции по настройке
```

**Для добавления навигации:**
```bash
npm install @react-navigation/native @react-navigation/native-stack
npm install react-native-screens react-native-safe-area-context
```

### src/services/

API и другие внешние сервисы.

```
services/
├── api.ts                     # HTTP клиент
├── storage.ts                 # Локальное хранилище (добавить по необходимости)
└── index.ts                   # Экспорты сервисов
```

**Пример использования:**
```typescript
import {api} from '@services';

const fetchUsers = async () => {
  const users = await api.get('/users');
  return users;
};
```

### src/utils/

Утилиты, константы, хелперы.

```
utils/
├── constants.ts               # Константы (цвета, размеры)
├── helpers.ts                 # Вспомогательные функции
└── index.ts                   # Экспорты утилит
```

**Использование:**
```typescript
import {COLORS, formatDate} from '@utils';
```

### src/types/

TypeScript типы и интерфейсы.

```
types/
├── index.ts                   # Глобальные типы
└── navigation.ts              # Типы навигации
```

### src/assets/

Статические ресурсы (изображения, шрифты).

```
assets/
├── images/                    # Изображения
│   ├── logo.png
│   └── ...
├── fonts/                     # Шрифты
│   └── ...
└── README.md                  # Инструкции по использованию
```

## 🔗 Алиасы путей

Настроены следующие алиасы в `tsconfig.json` и `babel.config.js`:

| Алиас | Путь |
|-------|------|
| `@/*` | `src/*` |
| `@components/*` | `src/components/*` |
| `@screens/*` | `src/screens/*` |
| `@navigation/*` | `src/navigation/*` |
| `@services/*` | `src/services/*` |
| `@utils/*` | `src/utils/*` |
| `@types/*` | `src/types/*` |
| `@assets/*` | `src/assets/*` |

**Пример:**
```typescript
// ❌ Плохо
import {Button} from '../../../components/Button';

// ✅ Хорошо
import {Button} from '@components';
```

## 📝 Соглашения об именовании

| Тип | Формат | Пример |
|-----|--------|--------|
| Компоненты | PascalCase | `Button.tsx` |
| Экраны | PascalCase + Screen | `HomeScreen.tsx` |
| Утилиты | camelCase | `helpers.ts` |
| Константы | UPPER_SNAKE_CASE | `API_BASE_URL` |
| Типы/Интерфейсы | PascalCase | `ButtonProps` |
| Функции | camelCase | `formatDate()` |
| Переменные | camelCase | `userName` |

## 🧩 Добавление новых модулей

### Новый компонент
1. Создайте `src/components/MyComponent.tsx`
2. Добавьте в `src/components/index.ts`: `export * from './MyComponent';`
3. Используйте: `import {MyComponent} from '@components';`

### Новый экран
1. Создайте `src/screens/MyScreen.tsx`
2. Добавьте в `src/screens/index.ts`: `export * from './MyScreen';`
3. Добавьте в навигацию (если используется)

### Новый сервис
1. Создайте `src/services/myService.ts`
2. Добавьте в `src/services/index.ts`: `export * as myService from './myService';`
3. Используйте: `import {myService} from '@services';`

## 🎨 Стилизация

### Рекомендуется:
- StyleSheet.create() для оптимизации
- Используйте константы из `@utils/constants`
- Следуйте принципу DRY

**Пример:**
```typescript
import {StyleSheet} from 'react-native';
import {COLORS, SIZES} from '@utils';

const styles = StyleSheet.create({
  container: {
    backgroundColor: COLORS.primary,
    padding: SIZES.md,
  },
});
```

## 🔄 Обновление структуры

При добавлении новых директорий:
1. Обновите `tsconfig.json` (paths)
2. Обновите `babel.config.js` (alias)
3. Обновите этот файл
4. Обновите README.md

---

**Важно:** Следуйте этой структуре для поддержки чистоты и масштабируемости проекта!

