# Contributing to RC2App

Мы рады вашему желанию внести вклад в проект! Вот несколько правил, которые помогут сделать этот процесс гладким.

## 🚀 Как начать

1. Форкните репозиторий
2. Клонируйте ваш форк:
```bash
git clone https://github.com/your-username/RC2App.git
cd RC2App
```

3. Создайте ветку для ваших изменений:
```bash
git checkout -b feature/amazing-feature
```

4. Установите зависимости:
```bash
npm install
```

## 📝 Правила кодирования

### TypeScript
- Всегда используйте строгую типизацию
- Избегайте использования `any`
- Определяйте интерфейсы для всех props

### Стиль кода
- Используйте Prettier для форматирования
- Следуйте правилам ESLint
- Запускайте `npm run lint` перед коммитом

### Компоненты
- Используйте функциональные компоненты с хуками
- Создавайте переиспользуемые компоненты в `src/components/`
- Документируйте props через TypeScript интерфейсы

### Именование
- Компоненты: `PascalCase` (например, `MyComponent.tsx`)
- Файлы утилит: `camelCase` (например, `helpers.ts`)
- Константы: `UPPER_SNAKE_CASE` (например, `API_BASE_URL`)

## 🧪 Тестирование

Перед отправкой PR убедитесь, что:
- Код проходит линтинг: `npm run lint`
- Код отформатирован: `npm run format`
- TypeScript проверка: `npm run type-check`
- Тесты проходят: `npm test`

## 📤 Отправка изменений

1. Закоммитьте изменения:
```bash
git add .
git commit -m "feat: добавлена новая фича"
```

2. Отправьте в ваш форк:
```bash
git push origin feature/amazing-feature
```

3. Создайте Pull Request

## 📋 Формат коммитов

Используйте conventional commits:
- `feat:` - новая функциональность
- `fix:` - исправление бага
- `docs:` - изменения в документации
- `style:` - форматирование кода
- `refactor:` - рефакторинг
- `test:` - добавление тестов
- `chore:` - обновление зависимостей и т.д.

Примеры:
```
feat: добавлен компонент Button
fix: исправлена ошибка в HomeScreen
docs: обновлен README
```

## ❓ Вопросы?

Если у вас есть вопросы, создайте issue в репозитории.

Спасибо за вклад! 🎉

